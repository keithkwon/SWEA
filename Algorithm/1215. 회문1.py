import sys
sys.stdin = open('input.txt')

total_tc = 10

for tc in range(1, total_tc+1):
    pali_len = int(input())
    row_origin = input()
    row=]
    for alphabet in row_origin:

    table = [list(input().split("")) for _ in range (8)]
    print(table)